name: Quality Checks

on:
  pull_request:
    branches:
      - main
      - dev

permissions: {}

jobs:
  test:
    uses: ./.github/workflows/test.yml

  lint:
    uses: ./.github/workflows/lint.yml
